     server {
        listen      80;
        server_name brew-monitor;

        root /app;

        location / {
            try_files $uri /index.html;
        }

        error_page 500 502 503 504 /50x.html;

        location =  / {
            root        /app;
            proxy_pass  http://localhost:3000;
        }
    }
