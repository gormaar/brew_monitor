name: Webhook 
on:
  workflow_run:
    workflows: ["CI"]
  workflow_dispatch:
    branches:
      - master
        dev
jobs:
  webhook:
    runs-on: ubuntu-latest
    steps:
      - if: ${{ github.event.workflow_run.conclusion == 'success' }}
        name: Build pipeline status check
      - name: Webhook Action
        uses: joelwmale/webhook-action@2.0.2
        with:
          url: ${{ secrets.WEBHOOK_URL }}
          #headers:
          #body:
